<?xml version="1.0" encoding="UTF-8"?>

<urlset xmlns="[suspicious link removed]"
xmlns:xhtml="http://www.w3.org/1999/xhtml">
{% for url in urlset %}
<url>
<!-- The required location tag (URL of the page) -->
<loc>{{ url.location }}</loc>

    <!-- Optional tags, rendered only if the sitemap object provides the data -->
    {% if url.lastmod %}<lastmod>{{ url.lastmod|date:"Y-m-d" }}</lastmod>{% endif %}
    {% if url.changefreq %}<changefreq>{{ url.changefreq }}</changefreq>{% endif %}
    {% if url.priority %}<priority>{{ url.priority }}</priority>{% endif %}
    
    <!-- For multilingual/regional versions of a page -->
    {% if url.alternates %}
        {% for lang, location in url.alternates %}
            <xhtml:link rel="alternate" hreflang="{{ lang }}" href="{{ location }}"/>
        {% endfor %}
    {% endif %}
</url>
{% endfor %}


</urlset>